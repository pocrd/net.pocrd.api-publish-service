services:
  dubbo-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-publish-service
    ports:
      - "50053:50053"
      - "50054:50054"
    environment:
      - DUBBO_REGISTRY_ADDRESS=nacos://host.docker.internal:30848
      - DUBBO_CONFIG_CENTER=nacos://host.docker.internal:30848
      - DUBBO_METADATA_REPORT_ADDRESS=nacos://host.docker.internal:30848
    networks:
      - higress_higress-net  # 连接到 Nacos 和 Higress 所在的网络
    restart: unless-stopped
    volumes:
      # 挂载 Maven 本地仓库，加速依赖下载
      - maven-repo:/root/.m2

volumes:
  maven-repo:
    driver: local

networks:
  higress_higress-net:
    external: true  # 使用已存在的网络
